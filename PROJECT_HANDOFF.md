# Project handoff

Updated 2026-09-11. The bounded ordinary-classic-payment reconciliation milestone is implemented, locally verified, independently buildable and pushed. Read docs/GAP_ASSESSMENT.md, ROADMAP.md and DECISIONS.md for scope and remaining work. Existing MIT licensing and three repository boundaries are preserved.

Implemented: exact positive int64-range stroop amounts; required network/operation/asset/issuer/direction identity; explicit reference checks; conservative duplicate/ambiguous/malformed/coverage handling; separately counted UNKNOWN results with candidate operation IDs; paginated Horizon reads, bounded retries, retention/freshness evidence and error propagation. No signing or fund movement.

Code revisions: 48e0036 (core/coverage), bbd6faf (competing claims), 89aa1ec (candidate IDs). CI/docs revision dda2427 contains the same runtime. All were normally pushed. Initial tree was clean at d4fe255; baseline tests passed but ingestion had no tests. The adjacent ProofGrid documents supplied engineering discipline only; its product, licensing, permissions and tests did not transfer.

Verification: Go 1.24.4 at C:/Go/bin/go.exe (initially absent from PATH). go test ./..., go vet ./... and go build ./... passed. An exported 89aa1ec source archive also passed with GOWORK=off and a fresh GOMODCACHE: 37 passing test cases including subtests. Tests cover maximum-range one-stroop differences, wrong identity/direction/issuer, failed operations, duplicates, ambiguity, missing coverage, >200 payments, later-page failure, retries and cancellation. A root-directory go test initially failed because the workspace root is not a module; checks run inside each repository.

Remote CI **passed** at dda2427: https://github.com/LedgerParity/ledger-parity-core/actions/runs/34654056907 (Go 1.22.2 and stable, two jobs, configured race tests/vet/build). Push runs initially did not appear. Authenticated inspection confirmed Actions enabled; manual dispatch succeeded without changing policy. Other verified runs: connectors 83fd7ec, CLI 1861a5f, linked in CLI docs/VERIFICATION.md. Initial unauthenticated API requests hit rate limits; public run pages and normal configured authentication resolved verification. This completion documentation commit does not change runtime or CI.

Local Windows -race could not start because CGO was disabled and no C compiler was available. A portable compiler download was cancelled after very slow progress and its partial archive removed; no compiler/global config was installed. Linux CI race execution is separately verified.

The CLI's deterministic offline demo gives 1 match, 4 discrepancies and 3 unknowns (exit 3). Its read-only public-testnet check passed at 2026-09-11T22:14:14Z; CLI docs/LIVE_TESTNET_RESULT.json explicitly states that the expected record was synthesized from a provider observation. This is not real application integration or production proof.

Self-review (not an independent-agent audit) caught unsafe reuse of duplicate claims, unreferenced amount-difference inference and missing candidate IDs. Regression tests cover the fixes. Legacy store APIs are not integrated: no durable atomic checkpoint/resume, bundled SQLite driver or complete report persistence. Also deferred: Soroban, path payments, account creation/merge settlement, StrKey validation, broader muxed scope, provider-independent history and scale optimization.

Next: validate practical usefulness with a consenting operator's sanitized export and known discrepancy. Named adapters remain experimental pending upstream contracts. Original Drips feedback/application are unavailable; maintainer recalls Stellar relevance/impact concerns. CLI contains a sourced appeal draft, not a submitted application. No adoption, partnership, quantified impact or acceptance is claimed. Final normal pushes/status are reported to the user; preserve these evidence distinctions in future work.

Follow-up research: docs/STELLAR_OPPORTUNITIES.md records pinned SDP export source findings, adjacent-platform comparison, RPC migration risk and implementation acceptance gates. No SDP adapter or RPC ingestor was added; source inspection is not executed upstream testing or operator validation. Next concrete task is a release-verified export contract with independent sender/network/time scope, followed by replayable evidence. Research did not change prior runtime test evidence.
